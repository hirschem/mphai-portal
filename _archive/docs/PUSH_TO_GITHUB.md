# After creating the repo on GitHub, run these commands:

# Connect to your GitHub repo (replace YOUR_USERNAME with your GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/mph-handwriting.git

# Rename branch to main (modern convention)
git branch -M main

# Push to GitHub
git push -u origin main

# You'll be prompted to login to GitHub - use your credentials or GitHub CLI
